<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Time Tracker</title>
    <!-- <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet"> -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="icon" type="image/png" sizes="32x32" href="/icon/title.png">

    <style>
        body {
            background-color: #f8f9fa;
        }
        .profile-card {
            margin-top: 20px;
            padding: 20px;
            border-radius: 15px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .time-entry {
            border-left: 4px solid #007bff;
            padding: 15px;
            margin-bottom: 20px;
            background-color: #f1f1f1;
            border-radius: 10px;
        }
        .time-entry h5 {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <%-include('include/navbar.ejs')%>
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
          
        </div>
    </div>
    
    <div class="row mt-5">
        <div class="col-md-8 offset-md-2">
            <h4>Time Tracking Data</h4>
            <% if (texts.length > 0) { %>
                <% texts.forEach(timeEntry => { %>
                    <div class="time-entry">
                        <h5><i class="far fa-clock"></i> Start: <%= timeEntry.startclock %></h5>
                        <h5><i class="far fa-clock"></i> End: <%= timeEntry.endclock %></h5>
                        <p>Total Hours: <%= timeEntry.totalhour %></p>
                        <p>Total Earning: $<%= timeEntry.totalearning.toFixed(2) %></p>
                        <small>Logged on: <%= new Date(timeEntry.createdAt).toDateString() %></small>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No time entries found.</p>
            <% } %>
        </div>
    </div>
</div>
<%-include('include/footer.ejs')%>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/nav.js" defer></script>
    
</body>
</html>
